<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="coorp.ah.mupi2">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission
        android:name="com.example.indraga.rute.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.example.indraga.rute.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListTempat"
            android:label="@string/title_activity_list_tempat"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SoalActivity"
            android:label="@string/title_activity_soal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SoalRinciActivity"
            android:label="@string/title_activity_soal_rinci"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".JalurActivity"
            android:label="@string/title_activity_jalur" />
        <activity
            android:name=".GambarSoalActivity"
            android:label="@string/title_activity_gambar_soal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Credit"
            android:label="@string/title_activity_credit"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AturanActivity"
            android:label="@string/title_activity_aturan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SoalKhusus"
            android:label="@string/title_activity_soal_khusus"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
